{
  "name": "queue-music",
  "version": "1.0.0",
  "description": "LAN party music queue player — central playback via yt-dlp, per-user queues, no build step.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^8.3.0",
    "better-sqlite3": "^11.10.0",
    "fastify": "^5.5.1",
    "preact": "^10.29.8",
    "yt-search": "^2.12.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "vite": "^8.2.2"
  }
}
